============
Installation
============

At the command line::

    $ easy_install keychain_manager

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv keychain_manager
    $ pip install keychain_manager